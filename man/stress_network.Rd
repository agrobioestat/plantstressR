% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stress_network.R
\name{stress_network}
\alias{stress_network}
\alias{print.plantstress_network}
\title{Partial-Correlation Network of Physiological Traits}
\usage{
stress_network(
  data,
  traits = NULL,
  treatment = NULL,
  level = NULL,
  method = c("partial", "pearson", "spearman"),
  lambda = "auto",
  threshold = 0.1,
  alpha = 0.05,
  p_adjust = "BH",
  cluster = c("louvain", "walktrap", "fast_greedy", "none"),
  sri = NULL
)

\method{print}{plantstress_network}(x, ...)
}
\arguments{
\item{data}{A data frame with one row per experimental unit.}

\item{traits}{Character vector of trait columns. Defaults to every numeric
column that is not a design column.}

\item{treatment}{Optional name of a treatment column used to restrict the
network to particular levels.}

\item{level}{Optional character vector of \code{treatment} levels to keep. A
network estimated only on stressed plants describes the stressed
physiology; one estimated on the whole trial mixes both regimes.}

\item{method}{\code{"partial"} (default), \code{"pearson"} or \code{"spearman"}. The last two
produce a marginal correlation network.}

\item{lambda}{Shrinkage intensity for the partial-correlation estimate:
\code{"auto"} or a scalar in \verb{[0, 1)}.}

\item{threshold}{Minimum absolute correlation retained as an edge.}

\item{alpha}{Adjusted p-value threshold for retaining an edge; ignored when
p-values cannot be computed.}

\item{p_adjust}{Multiple-testing adjustment passed to \code{\link[stats:p.adjust]{stats::p.adjust()}}.}

\item{cluster}{Module detection algorithm: \code{"louvain"}, \code{"walktrap"},
\code{"fast_greedy"} or \code{"none"}.}

\item{sri}{Optional \code{plantstress_sri} object. Its indices are averaged per
trait and attached to the node table as \code{sri}, so that node color can show
how each hub responded to stress.}

\item{x}{A \code{plantstress_network} object.}

\item{...}{Ignored.}
}
\value{
An object of class \code{"plantstress_network"}: a list with \code{nodes},
\code{edges}, \code{graph} (an \link[igraph:aaa-igraph-package]{igraph::igraph} object), \code{modules}, \code{matrix} (the
partial or marginal correlation matrix) and \code{parameters}.
}
\description{
Estimates the conditional dependence structure among physiological traits and
detects the modules of traits that respond to stress together. Unlike a
marginal correlation network, every edge here is the association between two
traits \emph{after removing the effect of all the other traits in the panel}, which
removes the dense clusters of indirect correlations that make raw correlation
networks unreadable.
}
\details{
The correlation matrix is shrunk towards the identity,
\eqn{R_\lambda = (1 - \lambda) R + \lambda I}, before inversion. With
\code{lambda = "auto"} the smallest intensity on a fixed grid that yields a
well-conditioned matrix is used, which keeps the estimate usable when the
number of traits approaches the number of plots. Partial correlations are then
read off the precision matrix as
\eqn{-\theta_{ij} / \sqrt{\theta_{ii}\theta_{jj}}}.

Significance uses \eqn{t = \rho \sqrt{(n - p) / (1 - \rho^2)}} on \eqn{n - p}
degrees of freedom, where \code{p} is the number of traits. When the design has
fewer observations than that requires, p-values are returned as \code{NA} and edge
selection falls back to \code{threshold} alone; the shrinkage also makes the test
approximate, so \code{threshold} should be treated as the primary filter.

Modules are found on the graph of absolute edge weights. \code{"louvain"}
(multi-level modularity) is the default; \code{"walktrap"} and \code{"fast_greedy"} are
available for comparison.
}
\examples{
data(brachiaria_stress)

net <- stress_network(
  brachiaria_stress,
  traits = c("Fv_Fm", "PIabs", "A", "gs", "E", "RWC", "SPAD", "shoot_biomass"),
  treatment = "drought_level",
  level = c("moderate", "severe")
)
net
head(net$edges)
}
\references{
Epskamp S., Fried E.I. (2018). A tutorial on regularized partial correlation
networks. \doi{10.1037/met0000167}

Blondel V.D., Guillaume J.-L., Lambiotte R., Lefebvre E. (2008). Fast
unfolding of communities in large networks.
\doi{10.1088/1742-5468/2008/10/P10008}
}
\seealso{
\code{\link[=plot_stress_signature]{plot_stress_signature()}}, \code{\link[=calculate_sri]{calculate_sri()}}
}
