% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stress_module_scores.R
\name{stress_module_scores}
\alias{stress_module_scores}
\title{Score the Network Modules of a Stress Signature}
\usage{
stress_module_scores(
  network,
  sri,
  aggregate = c("mean", "rms"),
  include_unassigned = FALSE
)
}
\arguments{
\item{network}{A \code{plantstress_network} object from \code{\link[=stress_network]{stress_network()}}.}

\item{sri}{A \code{plantstress_sri} object from \code{\link[=calculate_sri]{calculate_sri()}}, or a data frame
with the columns \code{unit}, \code{group}, \code{trait} and \code{sri}.}

\item{aggregate}{\code{"mean"} (signed) or \code{"rms"} (magnitude).}

\item{include_unassigned}{Logical. Keep traits that were not placed in any
module, pooled under the label \code{"unassigned"}.}
}
\value{
A tibble with columns \code{unit}, \code{group}, \code{module}, \code{label}, \code{n_traits},
\code{hub}, \code{score} and \code{traits}, sorted by decreasing score within each unit and
stress level.
}
\description{
Joins the two halves of the analysis. \code{\link[=stress_network]{stress_network()}} finds the modules of
traits that move together; \code{\link[=calculate_sri]{calculate_sri()}} measures how far each trait moved.
\code{stress_module_scores()} averages the second within the first, turning a
trait-level signature into a handful of module-level statements such as
"the photochemical module was impaired by 2.4 standard deviations under
severe deficit, the stomatal module by 1.1".
}
\details{
Only traits present in both objects are used; traits that were dropped from
the network, or that carry no module because they were isolated, are reported
in the \code{module} column as \code{NA} and excluded from the scores unless
\code{include_unassigned = TRUE}, in which case they are pooled into a module
labeled \code{"unassigned"}.

\code{aggregate = "mean"} keeps the sign, so a module can come out negative when
stress improved its traits. \code{aggregate = "rms"} returns the magnitude of the
disturbance and never cancels.
}
\examples{
data(brachiaria_stress)

traits <- c("Fv_Fm", "PIabs", "DI0_RC", "A", "gs", "Ci", "RWC", "SPAD")

sri <- calculate_sri(brachiaria_stress,
  treatment = "drought_level", control = "control",
  traits = traits, verbose = FALSE
)

net <- stress_network(brachiaria_stress,
  traits = traits, treatment = "drought_level",
  level = c("moderate", "severe")
)

stress_module_scores(net, sri)
}
\seealso{
\code{\link[=stress_network]{stress_network()}}, \code{\link[=calculate_sri]{calculate_sri()}}, \code{\link[=integrated_stress_index]{integrated_stress_index()}}
}
