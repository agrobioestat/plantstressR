% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_sri.R
\name{calculate_sri}
\alias{calculate_sri}
\alias{print.plantstress_sri}
\alias{summary.plantstress_sri}
\title{Stress Response Index (SRI) per Trait}
\usage{
calculate_sri(
  data,
  treatment,
  control,
  traits = NULL,
  by = NULL,
  block = NULL,
  direction = "auto",
  method = c("glass", "cohen", "hedges", "relative"),
  conf_level = 0.95,
  p_adjust = "BH",
  verbose = TRUE
)

\method{print}{plantstress_sri}(x, n = 10, ...)

\method{summary}{plantstress_sri}(object, alpha = 0.05, ...)
}
\arguments{
\item{data}{A data frame with one row per experimental unit, containing the
treatment column, the trait columns and, optionally, a grouping column.}

\item{treatment}{Name of the treatment column.}

\item{control}{Level of \code{treatment} treated as the control. Every other level
is treated as a stress level and compared against it.}

\item{traits}{Character vector of trait columns. Defaults to every numeric
column that is not a design column.}

\item{by}{Optional grouping column (typically genotype). Indices are computed
within each level, against that level's own control. More than one column
may be named -- \code{by = c("genotype", "site")} describes a multi-environment
trial and analyses each genotype at each site against the control of that
cell. The levels are pasted into a single \code{unit} label, which
\code{\link[=stress_stability]{stress_stability()}} splits back apart.}

\item{block}{Optional name of a block (replicate) column. When supplied, the
index is estimated from an additive \code{trait ~ treatment + block} model
within each unit, so that a replicate which happened to sit in a wetter
corner of the glasshouse shifts neither the difference nor the scale. The
scaling standard deviation becomes the residual (within-block) one, the
test is the model's t-test on the treatment contrast, and the whole trial
contributes to the residual degrees of freedom instead of each pair of
groups separately. Leave \code{NULL} for a completely randomized design.}

\item{direction}{How each trait responds to stress. Either \code{"auto"} (the
default, resolved by \code{\link[=trait_directions]{trait_directions()}}), a single string applied to all
traits, or a named vector of \code{"higher_is_better"} / \code{"lower_is_better"}
(equivalently \code{-1} / \code{1}).}

\item{method}{Effect-size definition: \code{"glass"}, \code{"cohen"}, \code{"hedges"} or
\code{"relative"}.}

\item{conf_level}{Confidence level for the interval around the index.}

\item{p_adjust}{Multiple-testing adjustment passed to \code{\link[stats:p.adjust]{stats::p.adjust()}}.}

\item{verbose}{Logical. Emit design warnings from \code{\link[=validate_stress_data]{validate_stress_data()}}.}

\item{x}{A \code{plantstress_sri} object.}

\item{n}{Number of rows to display.}

\item{...}{Ignored.}

\item{object}{A \code{plantstress_sri} object.}

\item{alpha}{Significance threshold applied to the adjusted p-values.}
}
\value{
An object of class \code{"plantstress_sri"}, a tibble with one row per
unit, stress level and trait, and columns \code{unit}, \code{group}, \code{trait},
\code{direction}, \code{n_control}, \code{n_stress}, \code{mean_control}, \code{sd_control},
\code{mean_stress}, \code{sd_stress}, \code{difference}, \code{effect}, \code{sri}, \code{se},
\code{se_sampling}, \code{conf_low}, \code{conf_high}, \code{statistic}, \code{p_value} and
\code{p_adj}. \code{unit} is \code{"overall"} when \code{by} is \code{NULL}.

\code{se} is the full standard error of the index and is what the confidence
limits use. \code{se_sampling} holds only the component due to sampling the
experimental units, with the scaling standard deviation treated as fixed;
it is the quantity \code{\link[=integrated_stress_index]{integrated_stress_index()}} uses for precision
weighting, because the full \code{se} grows with the effect itself and would
otherwise penalize exactly the traits that responded to the stress.
}
\description{
Converts each physiological trait into a Stress Response Index: the
standardized difference between a stress treatment and its own control,
re-signed so that \strong{positive values always mean stress-induced impairment},
whatever the direction of the trait.

The magnitude is a standardized effect size, which puts traits measured in
incomparable units (\code{Fv/Fm}, micromoles of CO2 per square meter per second,
grams of biomass) on a common, dimensionless scale and makes them additive in
\code{\link[=integrated_stress_index]{integrated_stress_index()}}.
}
\details{
With \eqn{m_s} and \eqn{m_c} the stress and control means and \eqn{s} the
scaling standard deviation, the available \code{method} values are:

\describe{
\item{\code{"glass"}}{\eqn{(m_s - m_c) / s_c}, Glass's delta. The control group
alone sets the scale, so a treatment that inflates variance does not
shrink the index. This is the default.}
\item{\code{"cohen"}}{\eqn{(m_s - m_c) / s_p}, with the pooled standard
deviation.}
\item{\code{"hedges"}}{Cohen's d with the small-sample bias correction, useful
with few replicates per plot.}
\item{\code{"relative"}}{\eqn{(m_s - m_c) / |m_c|}, the classical relative
change used in the stress-tolerance selection literature; interpretable
as a proportion but only defined for ratio-scale traits with a non-zero
control mean.}
}

The raw effect is then multiplied by \eqn{-1} for traits declared
\code{"higher_is_better"} and left unchanged for traits declared
\code{"lower_is_better"} (see \code{\link[=trait_directions]{trait_directions()}}). Confidence limits are normal
approximations built from the effect-size standard error; the reported
\code{p_value} comes from a Welch two-sample t-test on the raw trait and is
adjusted across all tests with \code{p_adjust}.
}
\examples{
data(brachiaria_stress)

sri <- calculate_sri(
  brachiaria_stress,
  treatment = "drought_level",
  control = "control",
  traits = c("Fv_Fm", "PIabs", "A", "gs", "RWC", "SPAD", "shoot_biomass")
)
sri

# Per genotype, with an explicit direction for a trait the dictionary
# cannot know about
sri_g <- calculate_sri(
  brachiaria_stress,
  treatment = "drought_level",
  control = "control",
  traits = c("Fv_Fm", "A", "RWC", "DI0_RC"),
  by = "genotype",
  direction = c(DI0_RC = "lower_is_better")
)
summary(sri_g)
}
\references{
Fischer R.A., Maurer R. (1978). Drought resistance in spring wheat cultivars.
I. Grain yield responses. \doi{10.1071/AR9780897}

Hedges L.V. (1981). Distribution theory for Glass's estimator of effect size
and related estimators. \doi{10.3102/10769986006002107}
}
\seealso{
\code{\link[=integrated_stress_index]{integrated_stress_index()}}, \code{\link[=plot_stress_signature]{plot_stress_signature()}},
\code{\link[=trait_directions]{trait_directions()}}, \code{\link[=validate_stress_data]{validate_stress_data()}}
}
