% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_stress_data.R
\name{validate_stress_data}
\alias{validate_stress_data}
\alias{print.plantstress_validation}
\title{Validate a Control Versus Stress Trait Table}
\usage{
validate_stress_data(
  data,
  treatment,
  control,
  traits = NULL,
  by = NULL,
  block = NULL,
  min_replicates = 3L,
  max_missing = 0.2,
  verbose = TRUE
)

\method{print}{plantstress_validation}(x, ...)
}
\arguments{
\item{data}{A data frame with one row per experimental unit.}

\item{treatment}{Name of the column holding the treatment factor.}

\item{control}{Level of \code{treatment} used as the control (reference) group.}

\item{traits}{Character vector of trait columns. Defaults to every numeric
column that is not a design column.}

\item{by}{Optional name of a grouping column (typically genotype, cultivar or
site), or a vector of several -- \code{c("genotype", "site")} for a
multi-environment trial. When supplied, every combination of levels must
contain both control and stress observations.}

\item{block}{Optional name of a block (replicate) column. When supplied, the
design is additionally checked for enough block levels, for treatments
confined to a single block (which cannot be separated from it) and for
empty treatment-by-block cells.}

\item{min_replicates}{Minimum number of observations expected per design
cell. Cells below this threshold are flagged.}

\item{max_missing}{Maximum acceptable proportion of missing values per trait.}

\item{verbose}{Logical. Emit the collected issues as warnings.}

\item{x}{A \code{plantstress_validation} object.}

\item{...}{Ignored.}
}
\value{
Invisibly, an object of class \code{"plantstress_validation"}: a list with
\code{ok}, \code{issues} (a tibble with columns \code{severity}, \code{check} and \code{message}),
\code{design} (replication per design cell), \code{traits}, \code{treatment}, \code{control},
\code{stress_levels}, \code{by} and \code{block}.
}
\description{
Runs the design checks that every other \code{plantstressR} function relies on,
before any index is computed: presence and type of the design columns,
existence of the control level, replication of each design cell, constant or
fully missing traits, and the amount of missing data per trait.

Hard problems (missing columns, an unknown control level, no numeric traits)
raise an error. Soft problems (low replication, constant traits, high
missingness) are collected and returned so that the caller can decide what to
do, and are emitted as warnings when \code{verbose = TRUE}.
}
\examples{
data(brachiaria_stress)
chk <- validate_stress_data(
  brachiaria_stress,
  treatment = "drought_level",
  control = "control",
  traits = c("Fv_Fm", "A", "RWC", "shoot_biomass"),
  by = "genotype"
)
chk
}
\seealso{
\code{\link[=calculate_sri]{calculate_sri()}}
}
