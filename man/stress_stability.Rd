% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stress_stability.R
\name{stress_stability}
\alias{stress_stability}
\alias{print.plantstress_stability}
\alias{plot.plantstress_stability}
\title{Stability of the Stress Response Across Environments}
\usage{
stress_stability(isi, environment = NULL, rank_by = c("tolerance", "severity"))

\method{print}{plantstress_stability}(x, ...)

\method{plot}{plantstress_stability}(x, ...)
}
\arguments{
\item{isi}{A \code{plantstress_isi} object from \code{\link[=integrated_stress_index]{integrated_stress_index()}},
computed from an SRI whose \code{by} named at least two columns.}

\item{environment}{Which of the \code{by} columns is the environment. Defaults to
the last one. The remaining columns identify the genotype.}

\item{rank_by}{\code{"tolerance"} (rank 1 = least affected, the default) or
\code{"severity"}. Ranks are recomputed within each environment and stress
level, so they describe the standing of a genotype among the material it
was actually grown beside.}

\item{x}{A \code{plantstress_stability} object.}

\item{...}{Ignored.}
}
\value{
An object of class \code{"plantstress_stability"}, a tibble with one row
per genotype and stress level and columns \code{unit}, \code{group}, \code{n_env},
\code{mean_isi}, \code{sd_isi}, \code{cv_isi}, \code{mean_rank}, \code{rank_min}, \code{rank_max},
\code{ecovalence} and \code{ecovalence_pct}, ordered by \code{group} and \code{mean_isi}.
}
\description{
Summarizes a multi-environment trial: how severely each genotype was
affected on average, and how consistently. A genotype that ranks first at one
site and last at the next is not the same proposition as one that ranks
second everywhere, and a single mean hides the difference.

Run \code{\link[=calculate_sri]{calculate_sri()}} with \code{by = c("genotype", "site")} -- or whatever names
the genotype and the environment -- then \code{\link[=integrated_stress_index]{integrated_stress_index()}}, then
this function.
}
\details{
The table is read per stress level, because a moderate and a severe
treatment are different environments in themselves and are never pooled.

\code{sd_isi} and \code{cv_isi} describe the spread of a genotype's index across
environments. \code{rank_min}, \code{rank_max} and \code{mean_rank} describe the spread of
its position, which is what selection actually acts on.

\code{ecovalence} is Wricke's contribution to the genotype-by-environment
interaction,
\deqn{W_i = \sum_j (X_{ij} - \bar{X}_{i.} - \bar{X}_{.j} + \bar{X}_{..})^2,}
computed on the integrated index rather than on yield. It is zero for a
genotype whose response tracks the environment mean exactly, and large for
one that reshuffles. \code{ecovalence_pct} expresses it as a share of the total
interaction, so the genotypes responsible for the instability of the trial
can be named. Both need at least two environments and two genotypes, and are
\code{NA} otherwise.

Genotypes missing from some environments are kept, with \code{n_env} reporting how
many they appeared in, but their ecovalence is \code{NA}: an unbalanced
genotype-by-environment table has no meaningful interaction decomposition.
}
\examples{
data(brachiaria_stress)

# Two sites, by splitting the blocks of the bundled trial.
met <- brachiaria_stress
met$site <- ifelse(met$block \%in\% c("B1", "B2"), "north", "south")

sri <- calculate_sri(met,
  treatment = "drought_level", control = "control",
  traits = c("Fv_Fm", "A", "RWC", "shoot_biomass"),
  by = c("genotype", "site"), verbose = FALSE
)

stress_stability(integrated_stress_index(sri))
}
\references{
Wricke G. (1962). Uber eine Methode zur Erfassung der okologischen Streubreite
in Feldversuchen. Zeitschrift fur Pflanzenzuchtung 47, 92-96.

Finlay K.W., Wilkinson G.N. (1963). The analysis of adaptation in a plant
breeding program. \doi{10.1071/AR9630742}
}
\seealso{
\code{\link[=integrated_stress_index]{integrated_stress_index()}}, \code{\link[=calculate_sri]{calculate_sri()}}
}
