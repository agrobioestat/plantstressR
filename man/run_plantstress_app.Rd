% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shiny_app.R
\name{run_plantstress_app}
\alias{run_plantstress_app}
\title{Launch the plantstressR Dashboard}
\usage{
run_plantstress_app(launch.browser = TRUE, ...)
}
\arguments{
\item{launch.browser}{Logical. Open the app in the default browser.}

\item{...}{Further arguments passed to \code{\link[shiny:runApp]{shiny::runApp()}}, such as \code{port} or
\code{host}.}
}
\value{
Invisibly \code{NULL}; called for the side effect of running the app.
}
\description{
Opens a Shiny application that walks through the whole package workflow
without writing any code: load a table, declare the design, and read the
signature, the ranking, the trait network, the ordination and the classical
tolerance indices from tabs.

The app is a front end, not a second implementation. Every number and every
figure it shows comes from the same exported functions documented in this
manual, so anything seen on screen can be reproduced from a script.
}
\details{
\code{shiny} is an optional dependency. Install it with \code{install.packages("shiny")}
if the app refuses to start. \code{bslib}, if present, is used for the theme.
}
\examples{
if (interactive() && requireNamespace("shiny", quietly = TRUE)) {
  run_plantstress_app()
}
}
\seealso{
\code{\link[=calculate_sri]{calculate_sri()}}, \code{\link[=integrated_stress_index]{integrated_stress_index()}}, \code{\link[=stress_network]{stress_network()}}
}
