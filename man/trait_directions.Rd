% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trait_directions.R
\name{trait_directions}
\alias{trait_directions}
\title{Declare the Stress Direction of Each Trait}
\usage{
trait_directions(
  traits,
  lower_is_better = NULL,
  higher_is_better = NULL,
  default = c("higher_is_better", "lower_is_better"),
  use_dictionary = TRUE
)
}
\arguments{
\item{traits}{Character vector of trait names.}

\item{lower_is_better}{Character vector of trait names whose increase denotes
stress damage. Overrides the built-in dictionary.}

\item{higher_is_better}{Character vector of trait names whose increase denotes
better performance. Overrides the built-in dictionary.}

\item{default}{Direction used for traits that are not matched by the
dictionary nor by the explicit arguments. One of \code{"higher_is_better"} or
\code{"lower_is_better"}.}

\item{use_dictionary}{Logical. When \code{TRUE} (default) a small dictionary of
well-established plant-physiology traits is consulted before \code{default} is
applied. Set to \code{FALSE} for full manual control.}
}
\value{
A named character vector, one element per trait, with values
\code{"higher_is_better"} or \code{"lower_is_better"}.
}
\description{
The Stress Response Index produced by \code{\link[=calculate_sri]{calculate_sri()}} is \emph{signed}: positive
values must always mean "the stress treatment impaired this trait". Whether a
numeric increase is good or bad depends on the trait, so every trait needs a
direction. \code{trait_directions()} builds and validates that named vector.

Traits declared as \code{"higher_is_better"} (net photosynthesis, \code{Fv/Fm},
relative water content, biomass) have their raw effect multiplied by \code{-1};
traits declared as \code{"lower_is_better"} (\code{F0}, \code{DI0/RC}, \code{Ci},
malondialdehyde, electrolyte leakage, canopy temperature) keep the raw sign.
}
\examples{
trait_directions(c("A", "Fv_Fm", "DI0_RC", "Ci", "shoot_biomass"))

# Override the dictionary for a trait measured in a non-standard way
trait_directions(c("A", "WUE"), lower_is_better = "WUE")
}
\references{
Strasser R.J., Tsimilli-Michael M., Srivastava A. (2004). Analysis of the
chlorophyll a fluorescence transient. \doi{10.1007/978-1-4020-3218-9_12}
}
\seealso{
\code{\link[=calculate_sri]{calculate_sri()}}
}
