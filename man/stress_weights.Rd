% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/integrated_stress_index.R
\name{stress_weights}
\alias{stress_weights}
\alias{stress_contributions}
\title{Weights and Trait Contributions of an Integrated Stress Index}
\usage{
stress_weights(x)

stress_contributions(x)
}
\arguments{
\item{x}{A \code{plantstress_isi} object.}
}
\value{
A tibble. \code{stress_weights()} has columns \code{trait} and \code{weight} (summing
to one); \code{stress_contributions()} has columns \code{unit}, \code{group}, \code{trait},
\code{sri}, \code{weight} and \code{contribution}.
}
\description{
Accessors for the two by-products of \code{\link[=integrated_stress_index]{integrated_stress_index()}}: the trait
weights actually used, and how much each trait contributed to each unit's
index. Both are the natural inputs for reporting why a genotype was ranked
where it was.
}
\examples{
data(brachiaria_stress)
sri <- calculate_sri(brachiaria_stress,
  treatment = "drought_level", control = "control",
  traits = c("Fv_Fm", "A", "RWC"), by = "genotype", verbose = FALSE
)
isi <- integrated_stress_index(sri)
stress_weights(isi)
}
\seealso{
\code{\link[=integrated_stress_index]{integrated_stress_index()}}
}
