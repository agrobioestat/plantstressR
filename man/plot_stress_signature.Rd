% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_stress_signature.R
\name{plot_stress_signature}
\alias{plot_stress_signature}
\title{Plot the Physiological Stress Signature}
\usage{
plot_stress_signature(
  x,
  type = c("heatmap", "radar"),
  traits = NULL,
  groups = NULL,
  units = NULL,
  top_n = NULL,
  significant_only = FALSE,
  alpha = 0.05,
  cluster = TRUE,
  show_significance = TRUE,
  limits = NULL,
  palette = c("#2C7BB6", "grey95", "#D7191C"),
  title = NULL,
  subtitle = NULL
)
}
\arguments{
\item{x}{A \code{plantstress_sri} object from \code{\link[=calculate_sri]{calculate_sri()}}, or a data frame
with the columns \code{unit}, \code{group}, \code{trait}, \code{sri} and, optionally, \code{p_adj}.}

\item{type}{\code{"heatmap"} (default) or \code{"radar"}.}

\item{traits}{Optional character vector restricting and not reordering the
traits shown.}

\item{groups}{Optional character vector restricting the stress levels shown.}

\item{units}{Optional character vector restricting the units shown.}

\item{top_n}{Optional. Keep only the \code{top_n} traits with the largest absolute
index.}

\item{significant_only}{Logical. Keep only traits significant at \code{alpha} in at
least one cell.}

\item{alpha}{Adjusted p-value threshold used by \code{significant_only} and by the
significance stars.}

\item{cluster}{Logical. Order traits by hierarchical clustering of their index
profiles instead of by mean index.}

\item{show_significance}{Logical. Print significance stars on the heat map.}

\item{limits}{Optional numeric vector of length two giving the fill limits.
By default the scale spans the observed range with the neutral color
anchored at zero. Set it explicitly, for example \code{c(-8, 8)}, to make several
figures directly comparable.}

\item{palette}{Character vector of length three with the low, mid and high
colors.}

\item{title, subtitle}{Optional plot annotations.}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object, which can be further modified with the
usual \code{+} syntax.
}
\description{
Draws the multivariate stress signature held in a \code{plantstress_sri} object as
either a diverging heat map (traits by stress level) or a radar plot (one
closed polygon per stress level). Both displays share the same color
convention: positive values (warm) mean the trait was impaired by stress,
negative values (cool) mean it was enhanced, and zero means the stress
treatment was indistinguishable from the control.
}
\details{
The heat map is the default because it scales to dozens of traits and several
stress levels, and because trait rows can be ordered by hierarchical
clustering, which makes co-responding traits visually adjacent. The radar
plot is intended for a small, curated panel (typically 5 to 12 traits) in
presentations and manuscript figures; it requires at least three traits.

When \code{x} contains several units (for example genotypes), the plot is
faceted by unit.
}
\examples{
data(brachiaria_stress)

sri <- calculate_sri(
  brachiaria_stress,
  treatment = "drought_level",
  control = "control",
  traits = c("Fv_Fm", "PIabs", "DI0_RC", "A", "gs", "RWC", "SPAD", "shoot_biomass"),
  verbose = FALSE
)

plot_stress_signature(sri)
plot_stress_signature(sri, type = "radar", top_n = 6)
}
\seealso{
\code{\link[=calculate_sri]{calculate_sri()}}, \code{\link[=integrated_stress_index]{integrated_stress_index()}}, \code{\link[=stress_network]{stress_network()}}
}
