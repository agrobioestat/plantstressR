% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stress_ordination.R
\name{stress_ordination}
\alias{stress_ordination}
\alias{print.plantstress_ordination}
\alias{plot.plantstress_ordination}
\title{Multivariate Ordination of the Trait Space}
\usage{
stress_ordination(
  data,
  treatment = NULL,
  traits = NULL,
  ncomp = 2L,
  scale = TRUE,
  engine = c("stats", "FactoMineR")
)

\method{print}{plantstress_ordination}(x, ...)

\method{plot}{plantstress_ordination}(x, components = c(1L, 2L), loadings = TRUE, ellipse = TRUE, ...)
}
\arguments{
\item{data}{A data frame with one row per experimental unit.}

\item{treatment}{Optional name of a column used to color and group samples.}

\item{traits}{Character vector of trait columns. Defaults to every numeric
column that is not a design column.}

\item{ncomp}{Number of components to retain.}

\item{scale}{Logical. Scale traits to unit variance.}

\item{engine}{\code{"stats"} (default) or \code{"FactoMineR"}.}

\item{x}{A \code{plantstress_ordination} object.}

\item{...}{Ignored.}

\item{components}{Integer vector of length two giving the components to draw.}

\item{loadings}{Logical. Draw trait loading arrows.}

\item{ellipse}{Logical. Draw a normal confidence ellipse per group.}
}
\value{
An object of class \code{"plantstress_ordination"}: a list with \code{scores},
\code{loadings}, \code{eigenvalues}, \code{traits}, \code{treatment} and \code{engine}.
}
\description{
Places individual plants in the multivariate trait space with a principal
component analysis, so that the separation between control and stress
treatments can be inspected at the sample level. It complements
\code{\link[=calculate_sri]{calculate_sri()}}, which summarizes treatments, and \code{\link[=stress_network]{stress_network()}}, which
summarizes traits.
}
\details{
Traits are centered and, by default, scaled to unit variance, which is
mandatory whenever the panel mixes units. Missing values are imputed with the
trait mean before decomposition; traits with zero variance are dropped with a
warning because they carry no ordination information.

The \code{"stats"} engine uses \code{\link[stats:prcomp]{stats::prcomp()}} and has no dependency beyond base
R. The \code{"FactoMineR"} engine calls \code{FactoMineR::PCA()} when that package is
installed, which is convenient if the rest of the analysis is being done with
the FactoMineR and factoextra toolchain; results are returned in the same
shape either way, and the engine silently falls back to \code{"stats"} when
FactoMineR is not available.
}
\examples{
data(brachiaria_stress)
ord <- stress_ordination(
  brachiaria_stress,
  treatment = "drought_level",
  traits = c("Fv_Fm", "PIabs", "A", "gs", "RWC", "SPAD", "shoot_biomass")
)
ord
plot(ord)
}
\references{
Le S., Josse J., Husson F. (2008). FactoMineR: an R package for multivariate
analysis. \doi{10.18637/jss.v025.i01}
}
\seealso{
\code{\link[=calculate_sri]{calculate_sri()}}, \code{\link[=stress_network]{stress_network()}}
}
